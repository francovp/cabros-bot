Analyze this alert and provide structured insights, sentiment, and technical levels based on the context.

Alert: {{alertContext}}

Instructions:
- **Output:** Respond *only* with a single, valid JSON object.
- **JSON schema:**
{
  "sentiment": "BULLISH" | "BEARISH" | "NEUTRAL",
  "sentiment_score": number (-1.0 to 1.0 — negative = bearish, positive = bullish),
  "insights": string[] (max 10 key points),
  "technical_levels": {
    "supports": string[],
    "resistances": string[]
  },
  "invalidation_level": string | number (optional),
  "target_level": string | number (optional),
  "setup_type": "breakout" | "mean_reversion" | "trend_continuation" | "reversal" (optional),
  "risk_reward_ratio": string | number (optional)
}

Score calibration:
- Use an absolute sentiment_score of 0.9+ only when multiple independent corroborating sources and aligned technicals support the alert.
- Use an absolute sentiment_score of 0.6-0.8 for mixed or partial corroboration.
- Use an absolute sentiment_score below 0.4 when evidence is thin, absent, or contradictory.

Risk metadata must be omitted when it cannot be inferred reliably. Never invent a price level or risk-reward estimate.
