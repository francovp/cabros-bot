interface:
  display_name: "Async Integration Review"
  short_description: "Catch timeout, retry, and worker gaps"
  default_prompt: "Use $async-integration-review before changing or reviewing workers, retries, timeouts, or external integrations."
